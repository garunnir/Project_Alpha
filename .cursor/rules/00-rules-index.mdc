---
description: 룰 인덱스·우선순위·위반 시 알림 프로토콜 (최상위, 항상 적용)
alwaysApply: true
---

# Rules Index (최상위)

## Priority
1. 이번 턴 사용자 지시(범위·완료조건·하지 말 것)
2. 이 파일의 **위반 알림** 의무
3. 아래 Always / Glob / On-demand 룰
4. `CLAUDE.md` · 활성 skill

## Always apply
| File | Concern |
|------|---------|
| `agent-scope.mdc` | 경로·범위 SSOT·MCP·완료 요약·Dist UI path gate |
| `collaboration-rules.mdc` | 모드 게이트·승인·Rule 1/3/4/5 |
| `clarify-before-guess.mdc` | 모호·갈림 — fail-closed·직설 재질문·추측 패치 금지 |
| `bug-fix-gate.mdc` | 버그·회귀 — 진단·땜빵 금지·완료 (전역 SSOT) |
| `senior-advisory.mdc` | 구조·파이프라인 — Optional 선임 메모, 승인 전 구현 금지 |
| `no-hardcoding.mdc` | 매직 넘버/복붙 SSOT |

**구현 완료 (Dist 편집 시):** 컴파일만 Done 금지 → `agent-completion-gate.mdc` · `.claude/checklists/agent-completion-gate.md`

## Glob (해당 경로·작업 시)
| File | Globs / Trigger |
|------|-----------------|
| `agent-completion-gate.mdc` | `Assets/Dist/**` 구현·리팩터·프리팹/씬 |
| `collaboration-unity.mdc` | `Assets/Dist/**` |
| `unity-stack.mdc` | `Assets/Dist/**` |
| `urp-rendering.mdc` | `Assets/Dist/Visual/**` · Dist 렌더/셰이더 스크립트 |
| `ui-prefab-layout.mdc` · `ui-font.mdc` | Dist UI 스크립트·UI 프리팹 (편집 전 전문 읽기) |
| `ui-prefab-bake.mdc` | Dist Editor·MCP UI bake 스크립트 |
| `vendor-packages.mdc` | `Assets/ProPixelizer/**` · `Assets/Plugins/**` · `Packages/**` |
| `inspector-exposure.mdc` | `Assets/Dist/Scripts/**` · `Assets/Dist/SOData/**` |
| `tile-chunk-streaming.mdc` | `Assets/Dist/Scripts/Map/**` |
| `camera-ground-view.mdc` | Camera · ChunkStreamer · CombatHitscan · 조준 픽 |
| `map-system.mdc` | Map 스크립트 · `docs/map/**` |
| `inventory-ui.mdc` | Inventory 스크립트·프리팹 · `docs/inventory/**` |
| `locomotion.mdc` | `Entity/Locomotion` · `docs/locomotion/**` |
| `arm-anim-layers.mdc` | CharacterAnimator · Combat 팔 · Editor/Anim · equipment docs |
| `legacy.mdc` | `Assets/Dist/Legacy/**` · `docs/legacy/**` |
| `game-time.mdc` | Time 스크립트·SO · `docs/time/**` |
| `editor-automation-hygiene.mdc` | Dist Editor `[MenuItem]` · Ensure/Setup/Patch · Data Definitions `+ Create` |

## On-demand (요청·해당 작업 시만 읽기)
| File | When |
|------|------|
| `migration-parity.mdc` | 경로 교체·하이브리드·기능 불변 최적화 |
| `epistemic-labels.mdc` | 버그 진단·원인·MCP/로그 (`bug-fix-gate.mdc`가 참조) |
| `collaboration-convergence-notes.mdc` | 문서 수렴 라운드 (비규범) |

## Dist UI path gate
UI 경로 **생성·수정** 시 (읽기만 제외): `ui-font.mdc` + `ui-prefab-layout.mdc` 전문 · Task에는 `.claude/checklists/dist-ui-gate.md` MUST 블록.

## Violation notify (MUST)
위반 발견 시 **즉시**: (1) 룰/섹션 (2) 위치 (3) 이유 한 줄 (4) 조치. 적용 전 알림. 범위 밖 기존 위반은 알림만.
